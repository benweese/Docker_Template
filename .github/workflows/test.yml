name: Test CI

on: [push]

jobs:

  build:
 
    runs-on: ubuntu-latest
 
    steps:
    - uses: actions/checkout@v1
    
action "build" {
  uses = "actions/docker/cli@master"
  args = "build -t user/repo ."
}
